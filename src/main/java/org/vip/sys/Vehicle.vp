   Cars.init();
    def Vehicle
        src.java.sys.Cars.HelloWord(titan = "titan called from vehicle class");
    end

   def ApplyBrake
        src.java.sys.Cars.HelloWord(titan = "titan applied break");
    end