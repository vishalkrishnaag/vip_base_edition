 change = null;
 speed = 0;
 def flush
 end
 def destroy
 end
 def init
    if speed == 0 {
       System.println("speed is not initialized , setting speed to 30");
       speed = 30;
     }
       System.println("hello world");
   end

   def set_speed
    end

   def some_method1
          System.println("not initiated properly");
          System.println("not initiated properly");
    end

   def apply_break
       System.print("break is applied");
    end

   def increment_speed
       speed = self.speed + 1 ;
       System.println("speed increased now speed is "+self.speed);
    end